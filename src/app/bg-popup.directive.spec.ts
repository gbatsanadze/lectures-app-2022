import { BgPopupDirective } from './bg-popup.directive';

describe('BgPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new BgPopupDirective();
    expect(directive).toBeTruthy();
  });
});
